name: Build and Push Docker Images

on:
  push:
    branches:
      - main # Or your primary branch

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: iamshadow666
          password: ${{ secrets.CR_PAT }}

      - name: Build and push web image
        uses: docker/build-push-action@v5
        with:
          context: .
          dockerfile: apps/web/Dockerfile
          push: true
          tags: ghcr.io/iamshadow666/botify-web:latest

      - name: Build and push wserver image
        uses: docker/build-push-action@v5
        with:
          context: .
          dockerfile: apps/wserver/Dockerfile
          push: true
          tags: ghcr.io/iamshadow666/botify-wserver:latest

      - name: Build and push socket image
        uses: docker/build-push-action@v5
        with:
          context: .
          dockerfile: apps/socket/Dockerfile
          push: true
          tags: ghcr.io/iamshadow666/botify-socket:latest
