{
  "name": "@repo/redis",
  "private": true,
  "scripts": {
    "dev": "dotenv -e ../../.env tsup redis.ts --format cjs,esm --dts --watch",
    "build": "DOTENV_CONFIG_PATH=../../.env tsup redis.ts --format cjs,esm --dts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "dotenv-cli": "^10.0.0",
    "tsup": "^8.0.2"
  },
  "exports": {
    ".": {
      "types": "./dist/redis.d.ts",
      "import": "./dist/redis.mjs",
      "require": "./dist/redis.js"
    }
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "ioredis": "^5.7.0",
    "@repo/typescript-config": "workspace:*"
  }
}
