{
  "name": "@botify/socket",
  "module": "socket.ts",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only server.ts",
    "test": "jest --detectOpenHandles --forceExit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/jest": "^30.0.0",
    "jest": "^30.1.1",
    "ts-jest": "^29.4.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "bullmq": "^5.56.9",
    "dotenv": "^17.2.1",
    "socket.io": "^4.8.1",
    "@repo/db": "workspace:*"
  }
}
